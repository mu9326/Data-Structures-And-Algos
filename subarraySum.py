from typing import List


class Solution:
    def subarraySum(self, nums: List[int], k: int) -> int:
        # initially, res, prefixSum, and currSum are 0

        # we iterate over the nums array
        # add each element to the currSum

        # calculate the diff between currSum and our target

        # if this difference exists in our hashmap, add it's frequency/value to the res

        # increase the freq of the curSum in the hashmap by 1 + whatever the freq of the diff is

        # return the res

        pass
